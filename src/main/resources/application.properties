spring.application.name=LBM

# Datasource settings
spring.datasource.url=jdbc:postgresql://junction.proxy.rlwy.net:11959/railway
spring.datasource.username=postgres
spring.datasource.password=FHSilDccwOfHSFuTLVcJYKcCGcusNWeB
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging Settings
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql=trace

# SpringDoc OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

# Spring Multipartfile support file
spring.servlet.multipart.max-file-size=512MB
spring.servlet.multipart.max-request-size=512MB
spring.servlet.multipart.enabled=true

# Settings file destination
video.upload.dir=${user.home}/videos